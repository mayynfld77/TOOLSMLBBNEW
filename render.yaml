services:
  - type: web
    name: mlbb-checker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app --workers 1 --threads 4 --timeout 120
    plan: free